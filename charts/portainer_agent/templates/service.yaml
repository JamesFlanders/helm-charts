apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app }}-service
  {{- if .Values.ip }}
  annotations:
    metallb.io/loadBalancerIPs: {{ .Values.ip }}
  {{- end }}
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 9001
      targetPort: {{ .Values.port }}
  selector:
    app: {{ .Values.app }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app }}-headless-service
spec:
  clusterIP: None
  selector:
    app: {{ .Values.app }}