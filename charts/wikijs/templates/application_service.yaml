apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app }}-application-service
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 3000
  selector:
    app: {{ .Values.app }}-application