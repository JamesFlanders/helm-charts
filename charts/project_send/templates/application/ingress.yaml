apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.app }}-application-ingress
  annotations:
    app.kubernetes.io/ingress.class: "traefik"
    cert-manager.io/cluster-issuer: "cert-manager-cluster-issuer"
spec:
  tls:
    - hosts:
        - filetransfer.caenen.net
      secretName: {{ .Values.app }}-caenen-net-tls
  rules:
    - host: filetransfer.caenen.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.app }}-application-service
                port:
                  number: 80